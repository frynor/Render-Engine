#include <ncurses.h>
#include <stdio.h>
#include <math.h>
#include <ncurses.h>
#include <stdlib.h>
#include <string.h>


#define PI 3.141592653f

#define MAX(a, b) (((a)>(b))? (a) : (b))
#define MIN(a, b) (((a)<(b))? (a) : (b))

#define WH (LINES)
#define WW (COLS)
